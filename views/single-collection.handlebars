{{> collection-info collection}}
<a href="/dashboard/edit/{{collection.id}}" class="edit-link">Edit collection</a>
{{#if loggedIn}}
<form class="scrunchie-form">
  <div>
    <textarea name="scrunchie-body"></textarea>
  </div>

  <div>
    <a href="./add-scrunchie.handlebars"><button type="submit" class="btn btn-success">Add scrunchie</button></a>
  </div>
</form>
{{/if}}

{{> scrunchies collection.scrunchies}}

{{#if loggedIn}}
<script src="/javascript/scrunchie.js"></script>
{{/if}}